/** Shared padding for all input and selector elements. Change here to update everywhere. */
export const INPUT_PADDING = '5px 8px';

/** Horizontal padding for the Settings screen, matches VS Code viewlet header indent. */
export const SETTINGS_PADDING_X = 20;
